package org.example.vetclinic.entity;

/**
 * Enum representing the status of a pet.
 * This enum is used to track the status of a pet in the system.
 */
public enum StatusPet {
    PRESENT,
    DELETED
}
