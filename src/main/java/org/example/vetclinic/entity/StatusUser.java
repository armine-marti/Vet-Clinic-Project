package org.example.vetclinic.entity;

/**
 * Enum representing the status of a user.
 * This enum is used to track the status of a user in the system.
 */
public enum StatusUser {
    ACTIVE,
    DELETED
}
