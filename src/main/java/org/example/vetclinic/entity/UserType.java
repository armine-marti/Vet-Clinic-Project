package org.example.vetclinic.entity;
/**
 * Enum representing the type of a user.
 * This enum is used to differentiate between different user roles in the system.
 */
public enum UserType {

    USER,
    ADMIN
}
