package org.example.vetclinic.entity;

/**
 * Enum representing the size of a pet.
 * This enum is used to define the different size categories for pets.
 */
public enum Size {
    SMALL,
    MEDIUM,
    LARGE
}
